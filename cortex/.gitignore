# Dépendances
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Tests
/coverage

# Next.js
/.next/
/out/
/build
next-env.d.ts
*.tsbuildinfo

# Divers
.DS_Store
*.pem
.vercel

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Variables d'environnement — aucun secret dans le dépôt
.env*
!.env.example

# Données locales : bases SQLite, documents importés, examens générés.
# Rien n'est versionné (voir CONTRIBUTING.md, invariant 8).
data/

# Sauvegardes (npm run backup)
backups/
